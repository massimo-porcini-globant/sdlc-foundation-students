# src/

This is where your personal finance manager code goes.

The internal structure is up to you — decide it based on your stack:

- Python: `src/finance/`, `src/cli/`, `tests/`, etc.
- Node: `src/`, `lib/`, `tests/`, etc.
- Go: idiomatic Go layout
- Other: whatever fits best

Once you start writing code, replace this README with one of your own that documents your project.
